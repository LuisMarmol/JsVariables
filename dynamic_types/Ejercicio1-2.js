var var1 = 5;
console.log(var1);

var1 = '10';
console.log(var1);

//Aquí observamos cómo no solo se puede actualizar el valor de una variable en un dato del mismo tipo.
//Sino cómo también podemos cambiar el valor de una variables tipo number. Por una variable de tipo string.